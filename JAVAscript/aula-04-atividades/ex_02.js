let pessoa = {
    nome:"Eduardo",
    idade:16,
    altura:1.64 ,
    estudante:"verdadeiro"
}

console.log(`verificar se ${pessoa.nome} de ${pessoa.idade} anos , com ${pessoa.altura} de altura realmente é um estudante = ${pessoa.estudante}`)